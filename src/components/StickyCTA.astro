<a 
  id="sticky-reserve-btn"
  href="#reservation" 
  class="fixed bottom-8 right-8 z-40 bg-brand-500 text-white px-6 py-4 rounded-full font-bold shadow-2xl hover:bg-brand-600 hover:scale-105 transition-all duration-500 flex items-center gap-3 animate-bounce-slow border-4 border-white/20 backdrop-blur-sm opacity-100 translate-y-0"
>
  <span class="text-xl">🍰</span>
  <span class="hidden sm:inline">Reserveer je genietmomentje</span>
  <span class="sm:hidden">Reserveren</span>
</a>

<script>
  const stickyBtn = document.getElementById('sticky-reserve-btn');
  const contactSection = document.getElementById('contact');

  if (stickyBtn && contactSection) {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          stickyBtn.classList.add('opacity-0', 'pointer-events-none', 'translate-y-12');
          stickyBtn.classList.remove('opacity-100', 'translate-y-0');
        } else {
          stickyBtn.classList.remove('opacity-0', 'pointer-events-none', 'translate-y-12');
          stickyBtn.classList.add('opacity-100', 'translate-y-0');
        }
      });
    }, {
      threshold: 0.1
    });

    observer.observe(contactSection);
  }
</script>

<style>
  .animate-bounce-slow {
    animation: bounce 3s infinite;
  }
  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
</style>
