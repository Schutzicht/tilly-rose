<section class="relative h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Image with Parallax Effect -->
  <div class="absolute inset-0 z-0">
    <div 
      class="absolute inset-0 bg-cover bg-center bg-no-repeat transition-transform duration-1000 scale-105"
      style="background-image: url('/images/photo-1.webp');"
    >
      <div class="absolute inset-0 bg-gradient-to-br from-black/80 via-black/40 to-black/80"></div>
    </div>
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-4 max-w-5xl mx-auto space-y-8">
    <h2 class="text-brand-100 font-display text-lg md:text-2xl tracking-[0.2em] uppercase opacity-90 mb-4 animate-fade-in-up text-shadow">
      Welkom bij Tilly Rose
    </h2>
    <h1 class="text-4xl xs:text-5xl md:text-7xl lg:text-9xl font-display font-bold text-white leading-tight md:leading-none drop-shadow-2xl animate-fade-in-up delay-100">
      Proef de sfeer,<br/>
      <span class="text-brand-200 block mt-2">beleef de magie.</span>
    </h1>
    <p class="text-lg md:text-2xl text-white font-medium max-w-3xl mx-auto mt-6 md:mt-8 animate-fade-in-up delay-200 drop-shadow-md px-2">
      De gezelligste High Tea en Lunchroom van Vlissingen. Geniet van huisgemaakte lekkernijen in een warme, vintage ambiance.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center mt-10 animate-fade-in-up delay-300">
      <a 
        href="#reservation" 
        class="px-8 py-3 bg-brand-400 text-white font-medium text-lg rounded-full hover:bg-brand-500 hover:scale-105 transition-all shadow-lg"
      >
        Tafel Reserveren
      </a>
      <a 
        href="#menu" 
        class="open-menu-modal px-8 py-3 bg-white/10 backdrop-blur-sm border border-white/30 text-white font-medium text-lg rounded-full hover:bg-white/20 hover:scale-105 transition-all"
        data-menu-tab="broodjes"
      >
        Bekijk Menu
      </a>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <a href="#about" class="text-white/80 hover:text-white transition-colors">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </a>
  </div>
</section>

<style>
  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out forwards;
    opacity: 0;
    transform: translateY(20px);
  }
  
  .delay-100 { animation-delay: 0.2s; }
  .delay-200 { animation-delay: 0.4s; }
  .delay-300 { animation-delay: 0.6s; }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
